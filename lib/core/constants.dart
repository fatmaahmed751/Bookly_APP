import 'package:flutter/material.dart';

class AppColors {
static const kPrimaryColor = Color(0xffFFFFFF);
static var primaryColor = const Color(0xff5abd8c);
static const mainColor = Color(0xff212621);
static const textColor = Color(0xff242126);
static var secondTextColor =  Colors.grey[500];
static var authorColor =  Colors.grey[900]!;

  //(0xff5ABD8C);
}